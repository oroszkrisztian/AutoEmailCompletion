<UserControl x:Class="EmailCompleteApp.Pages.HistoryPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <!-- Colors -->
        <SolidColorBrush x:Key="BackgroundColor" Color="#F8F9FA"/>
        <SolidColorBrush x:Key="SurfaceColor" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="PrimaryColor" Color="#007BFF"/>
        <SolidColorBrush x:Key="TextColor" Color="#212529"/>
        
        <!-- Converter for visibility -->
        <BooleanToVisibilityConverter x:Key="BoolToVisibility"/>
        
        <!-- DataGrid Row Style - Better spacing and hover effect -->
        <Style x:Key="DataGridRowStyle" TargetType="DataGridRow">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Height" Value="48"/>
            <Setter Property="MinHeight" Value="48"/>
            <Setter Property="Margin" Value="0,2"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E8F4FF"/>
                </Trigger>
                <MultiTrigger>
                    <MultiTrigger.Conditions>
                        <Condition Property="IsMouseOver" Value="False"/>
                        <Condition Property="AlternationIndex" Value="1"/>
                    </MultiTrigger.Conditions>
                    <Setter Property="Background" Value="#FAFBFC"/>
                </MultiTrigger>
            </Style.Triggers>
        </Style>
        
        <!-- DataGrid Cell Style - Better padding and spacing -->
        <Style x:Key="DataGridCellStyle" TargetType="DataGridCell">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Background="{TemplateBinding Background}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Foreground" Value="{StaticResource TextColor}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <!-- DataGrid Column Header Style -->
        <Style x:Key="DataGridColumnHeaderStyle" TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#F8F9FA"/>
            <Setter Property="Foreground" Value="#495057"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Height" Value="44"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="BorderThickness" Value="0,0,0,2"/>
            <Setter Property="BorderBrush" Value="#DEE2E6"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
    </UserControl.Resources>

    <Grid Background="{StaticResource BackgroundColor}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="20,20,20,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Page Title -->
            <TextBlock Grid.Column="0"
                       Text="Istoric Comenzi"
                       FontSize="24"
                       FontWeight="Bold"
                       Margin="0,0,20,0"
                       VerticalAlignment="Center"
                       Foreground="{StaticResource TextColor}"/>

            <!-- Order Number Search Box -->
            <Border Grid.Column="1" 
                    Background="White"
                    BorderBrush="#DEE2E6"
                    BorderThickness="1"
                    CornerRadius="4"
                    Padding="8,4"
                    MinWidth="180"
                    VerticalAlignment="Center"
                    Margin="0,0,10,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBlock Text="Nr.:" 
                              FontSize="12" 
                              Foreground="#6C757D"
                              VerticalAlignment="Center"
                              Margin="0,0,6,0"
                              Grid.Column="0"/>
                    
                    <TextBox Grid.Column="1"
                            Text="{Binding OrderNumberSearchText, UpdateSourceTrigger=PropertyChanged}"
                            Background="Transparent"
                            BorderThickness="0"
                            FontSize="13"
                            VerticalContentAlignment="Center"
                            Height="24">
                        <TextBox.Style>
                            <Style TargetType="TextBox">
                                <Setter Property="Foreground" Value="{StaticResource TextColor}"/>
                                <Style.Triggers>
                                    <Trigger Property="Text" Value="">
                                        <Setter Property="Background">
                                            <Setter.Value>
                                                <VisualBrush Stretch="None" AlignmentX="Left">
                                                    <VisualBrush.Visual>
                                                        <TextBlock Text="nr. comandă" 
                                                                  Foreground="#AAAAAA"
                                                                  FontSize="13"
                                                                  Margin="2,0,0,0"/>
                                                    </VisualBrush.Visual>
                                                </VisualBrush>
                                            </Setter.Value>
                                        </Setter>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </TextBox.Style>
                    </TextBox>
                </Grid>
            </Border>

            <!-- Client Name Search Box -->
            <Border Grid.Column="2" 
                    Background="White"
                    BorderBrush="#DEE2E6"
                    BorderThickness="1"
                    CornerRadius="4"
                    Padding="8,4"
                    MinWidth="220"
                    VerticalAlignment="Center"
                    Margin="0,0,20,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBlock Text="Client:" 
                              FontSize="12" 
                              Foreground="#6C757D"
                              VerticalAlignment="Center"
                              Margin="0,0,6,0"
                              Grid.Column="0"/>
                    
                    <TextBox Grid.Column="1"
                            Text="{Binding ClientNameSearchText, UpdateSourceTrigger=PropertyChanged}"
                            Background="Transparent"
                            BorderThickness="0"
                            FontSize="13"
                            VerticalContentAlignment="Center"
                            Height="24">
                        <TextBox.Style>
                            <Style TargetType="TextBox">
                                <Setter Property="Foreground" Value="{StaticResource TextColor}"/>
                                <Style.Triggers>
                                    <Trigger Property="Text" Value="">
                                        <Setter Property="Background">
                                            <Setter.Value>
                                                <VisualBrush Stretch="None" AlignmentX="Left">
                                                    <VisualBrush.Visual>
                                                        <TextBlock Text="nume client" 
                                                                  Foreground="#AAAAAA"
                                                                  FontSize="13"
                                                                  Margin="2,0,0,0"/>
                                                    </VisualBrush.Visual>
                                                </VisualBrush>
                                            </Setter.Value>
                                        </Setter>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </TextBox.Style>
                    </TextBox>
                </Grid>
            </Border>
        </Grid>

        <!-- Main Content -->
        <Grid Grid.Row="1" Margin="20,20,20,20">
            <Border Background="{StaticResource SurfaceColor}"
                    CornerRadius="8"
                    Padding="20">
                <DataGrid x:Name="HistoryDataGrid"
                          ItemsSource="{Binding HistoryData}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          CanUserResizeRows="False"
                          SelectionMode="Single"
                          SelectionUnit="FullRow"
                          GridLinesVisibility="None"
                          HeadersVisibility="Column"
                          Background="White"
                          BorderThickness="0"
                          HorizontalScrollBarVisibility="Auto"
                          AlternationCount="2"
                          MouseDoubleClick="HistoryDataGrid_MouseDoubleClick"
                          RowStyle="{StaticResource DataGridRowStyle}"
                          CellStyle="{StaticResource DataGridCellStyle}"
                          ColumnHeaderStyle="{StaticResource DataGridColumnHeaderStyle}">
                    <DataGrid.ContextMenu>
                        <ContextMenu>
                            <MenuItem Header="Open Document" Click="OpenDocument_Click">
                                <MenuItem.Icon>
                                    <TextBlock Text="📄" FontSize="14"/>
                                </MenuItem.Icon>
                            </MenuItem>
                            <MenuItem Header="Edit" Click="EditOrder_Click">
                                <MenuItem.Icon>
                                    <TextBlock Text="✏️" FontSize="14"/>
                                </MenuItem.Icon>
                            </MenuItem>
                        </ContextMenu>
                    </DataGrid.ContextMenu>
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Nr. Comandă" Binding="{Binding NumarComanda}" Width="Auto" MinWidth="100"/>
                        <DataGridTextColumn Header="Client" Binding="{Binding ClientName}" Width="*" MinWidth="150"/>
                        <DataGridTextColumn Header="Rută" Binding="{Binding Route}" Width="*" MinWidth="180"/>
                        <DataGridTextColumn Header="Data Încărcare" Binding="{Binding DisplayDateLoaded}" Width="Auto" MinWidth="110"/>
                        <DataGridTextColumn Header="Data Descărcare" Binding="{Binding DisplayDateUnloaded}" Width="Auto" MinWidth="120"/>
                        <DataGridTextColumn Header="Tarif Client" Binding="{Binding ClientTarif}" Width="Auto" MinWidth="100"/>
                        <DataGridTextColumn Header="Tarif Transportator" Binding="{Binding TransportatorTarifDisplay}" Width="Auto" MinWidth="140"/>
                        <DataGridTextColumn Header="Data Creare" Binding="{Binding DisplayDateCreatedAt}" Width="Auto" MinWidth="110"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>

            <!-- Loading Overlay -->
            <Border Background="#80000000"
                    Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibility}}">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <ProgressBar IsIndeterminate="True" Width="200" Height="20"/>
                    <TextBlock Text="Se încarcă..." Foreground="White" Margin="0,8,0,0" HorizontalAlignment="Center" FontSize="14"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
