<Window x:Class="EmailCompleteApp.Windows.LoadingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Loading"
        Height="250"
        Width="400"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        Topmost="True">
    
    <Border Background="White" 
            CornerRadius="10" 
            BorderBrush="#007BFF" 
            BorderThickness="2"
            Effect="{DynamicResource DropShadowEffect}">
        <Border.Resources>
            <DropShadowEffect x:Key="DropShadowEffect" 
                              Color="Black" 
                              Direction="315" 
                              ShadowDepth="5" 
                              Opacity="0.3" 
                              BlurRadius="10"/>
        </Border.Resources>
        
        <Grid Margin="40">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="20"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="20"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="20"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <!-- App Title -->
            <TextBlock Grid.Row="0"
                       Text="Email Complete App"
                       FontSize="24"
                       FontWeight="Bold"
                       Foreground="#007BFF"
                       HorizontalAlignment="Center"/>
            
            <!-- Loading Spinner -->
            <Viewbox Grid.Row="2" Width="50" Height="50">
                <Grid>
                    <Ellipse Width="50" Height="50" 
                             Stroke="#007BFF" 
                             StrokeThickness="4" 
                             Opacity="0.3"/>
                    <Ellipse Width="50" Height="50" 
                             Stroke="#007BFF" 
                             StrokeThickness="4" 
                             StrokeDashArray="31.4 31.4"
                             RenderTransformOrigin="0.5,0.5">
                        <Ellipse.RenderTransform>
                            <RotateTransform x:Name="SpinnerRotation"/>
                        </Ellipse.RenderTransform>
                        <Ellipse.Triggers>
                            <EventTrigger RoutedEvent="Loaded">
                                <BeginStoryboard>
                                    <Storyboard RepeatBehavior="Forever">
                                        <DoubleAnimation Storyboard.TargetName="SpinnerRotation"
                                                         Storyboard.TargetProperty="Angle"
                                                         From="0" To="360"
                                                         Duration="0:0:1"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </Ellipse.Triggers>
                    </Ellipse>
                </Grid>
            </Viewbox>
            
            <!-- Loading Text -->
            <TextBlock Grid.Row="4"
                       x:Name="LoadingText"
                       Text="Loading data..."
                       FontSize="16"
                       Foreground="#666"
                       HorizontalAlignment="Center"/>
            
            <!-- Progress Information -->
            <TextBlock Grid.Row="6"
                       x:Name="ProgressText"
                       Text="Please wait while we initialize the application"
                       FontSize="12"
                       Foreground="#999"
                       HorizontalAlignment="Center"
                       TextWrapping="Wrap"
                       TextAlignment="Center"/>
        </Grid>
    </Border>
</Window>